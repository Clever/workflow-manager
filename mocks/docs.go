// Package mocks holds generated code for mocks of dependencies, for testing.
// We don't commit the atual mocks, but committing an empty package helps bootstrap setting up dependencies
// (i.e. without this file, go mod vendor fails on a fresh clone since tests import this package)
package mocks
